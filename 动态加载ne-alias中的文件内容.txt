  var aliasPromise = $$.run(aliasConf).then(function(conf) {
    conf && extend(true, pathAlias, conf);
  });

  ->$$.run
    ->amdLoader.get(name)
        ->amdLoader.makeDefer(name)
            ->ajax.require()
                =>(script onload后执行目标script中的)define
                    defineQueue.push(def);
                =>(在执行完动态加载的script后)amdLoader.postDefine
                    exports[file] = 加载的对象
                    resolve(对象)
                =>调用一开始的then:conf && extend(true, pathAlias, conf);
